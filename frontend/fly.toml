# fly.toml app configuration file generated for lstalk-frontend-morning-sky-4784 on 2026-03-22T23:49:20Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'lstalk-frontend-morning-sky-4784'
primary_region = 'ewr'

[build]
  context = "."
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
